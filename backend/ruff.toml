line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "I", "B", "UP", "C4", "SIM", "RET"]
ignore = ["B008"]   # FastAPI's Depends()/File() in defaults is the idiom

[lint.per-file-ignores]
"tests/*" = ["E402"]
